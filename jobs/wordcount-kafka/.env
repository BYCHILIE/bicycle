# Kafka Configuration for wordcount-kafka job
#
# Flow: socket_source -> kafka_sink_1 -> kafka_source_1 -> word_count -> kafka_sink_2 -> socket_sink
#
# These env vars are read by the wordcount-kafka binary at graph-build time.
# The values end up in the Kafka connector properties of the job graph JSON.
#
# For local development (cargo run):
#   KAFKA_BROKERS=localhost:9092
#
# For Docker cluster (docker compose --profile kafka):
#   KAFKA_BROKERS=kafka:9092
#
# Quick start:
#   1. Start cluster:     docker compose --profile kafka up -d
#   2. Create topics:     ./jobs/docker/create_topics.sh kafka:9092
#   3. Submit job:        docker compose --profile cli run cli submit bin/wordcount-kafka --plugin lib/libwordcount_kafka.so
#   4. Send input:        nc localhost 9999
#   5. Receive output:    nc localhost 9998

# Kafka broker address (use kafka:9092 inside Docker, localhost:9092 for local dev)
KAFKA_BROKERS=kafka:9092

# Topic for raw input lines (socket source -> kafka)
KAFKA_TOPIC_RAW=raw-lines

# Topic for word count results (word counter -> kafka)
KAFKA_TOPIC_COUNTS=word-counts

# Consumer group ID for reading raw lines
KAFKA_GROUP_ID=wordcount-kafka-group

# Socket source port (input)
SOURCE_PORT=9999

# Socket sink port (output)
SINK_PORT=9998

# Default parallelism
PARALLELISM=2
